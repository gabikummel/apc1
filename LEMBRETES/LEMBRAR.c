 //para compilar coloque os codigos 
ls (identifica as opções)
cd (escolhe a opção)
gcc programa -o a.out (compila)
./a.out (executa)

pra voltar o comando "cd .."

-------------------------
ORAÇÃO
#include <stdio.h>

int main () {




  return 0;
}
--------------------------


  Switch é pra colocar os cases, nao estabelece limite/parada (entradas)
  Break para os cases (limita)
ex: case printf (...); break;